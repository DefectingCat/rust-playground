在使用 Code runner 这个插件时，在 Linux 环境下会在当前目录下产生一个没有后缀名的二进制执行文件。也就是当前文件的编译后的产物，这个二进制文件当然是不想且没有必要被 Git track 的。

研究了一下发现 `.gitignore` 应该这么写：

在 git 1.8.2 及以后可以使用 `**` 通配符来匹配任何级别的子目录：

```bash
**/src/main
```